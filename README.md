# xmrig

Quick and dirty example of mining Monero (XMR) in kubernetes.

Will run by default as a daemonset on worker (non control plane) nodes as the most background low priority possible in kubernetes and the nodes linux scheduler.
